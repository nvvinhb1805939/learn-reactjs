const hobby = (state, action) => {
  switch (action.type) {
    case 'ADD__HOBBY':
      return state;
    case 'SET_ACTIVE__HOBBY':
      return state;
    default:
      return state;
  }
};

export default hobby;
